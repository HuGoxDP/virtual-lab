{
  "name": "virtual-lab-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Browser tests against the composed stack — the layer the unit suites cannot reach.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^26.2.0"
  }
}
